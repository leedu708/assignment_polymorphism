<h1>Graduate Programs</h1>

<ul>

  <% @programs.each do |program| %>
    <li>
      <%= link_to "#{program.id}: #{program.name}", graduate_program_students_path(program.id) %>
    </li>
  <% end %>

</ul>

<%= link_to "Undergraduate Programs", undergraduate_programs_path %>